# Word Game

Um jogo de palavras similar ao Termo (https://www.nytimes.com/games/wordle/index.html), Developed using React, TypeScript, Tailwind CSS, and OpenAI integration.

## Features

-  Word game with 6 attempts
-  Words generated by OpenAI
-  5 tips provided by the AI ​​(starting from the second mistake)
-  Scoring and ranking system
-  PostgreSQL database for storing users and scores.
-  Modern interface with Tailwind CSS

## Technologies

### Frontend
- React
- TypeScript
- Tailwind CSS 
- Vite

### Backend
- Node.js
- Express
- SQL puro
- Driver PostgreSQL
- OpenAI API

## Installation

### Backend

1. Navigate to the Backend folder.:
```bash
cd Backend
```

2. Install the dependencies.:
```bash
npm install
```

3. Create a `.env` file in the Backend folder containing your OpenAI key.:
```env
OPENAI_API_KEY=sua_chave_aqui
PORT=3001
```

4. Start the server.:
```bash
npm run dev
```

The server is running on `http://localhost:3001`

### Frontend

1. Navigate to the Frontend folder.:
```bash
cd Frontend
```

2. Install the dependencies.:
```bash
npm install
```

3. Start the development server.:
```bash
npm run dev
```

The frontend will be running on `http://localhost:5173` (or another port if 5173 is busy)

## How to Play

1. Enter your username.
2. OpenAI will generate a 5-letter word.
3. Try to guess the word in up to 6 attempts.
4. The colors indicate:
   - 🟢 Green: Correct letter in the correct position.
   - 🟡 Yellow: The letter is in the word but in the wrong position.
   - ⚫ Gray: The letter is not in the word.
5. Starting with the second incorrect attempt, you will receive tips from the AI.
6. Earn points by guessing the word correctly!

## Estrutura do Banco de Dados

O banco de dados SQLite contém duas tabelas:

### users
- `id` (UUID PRIMARY KEY)
- `username` (TEXT UNIQUE)
- `points` (INTEGER)
- `created_at` (DATETIME)

### games
- `id` (UUID PRIMARY KEY)
- `user_id` (UUID)
- `word` (TEXT)
- `won` (INTEGER)
- `attempts` (INTEGER)
- `created_at` (DATETIME)

## API Endpoints

- `POST /api/user` - Create or obtain user
- `POST /api/game/start` - Start a new game
- `POST /api/game/guess` - make an attempt
- `GET /api/user/:userId/points` - Get user score
- `GET /api/ranking` - Get ranked (top 10)

## Score

- 1ª attempt: 100 points
- 2ª attempt: 85 points
- 3ª attempt: 70 points
- 4ª attempt: 55 points
- 5ª attempt: 40 points
- 6ª attempt: 25 points

## notes

- The words are generated by OpenAI and may contain accents (which are automatically removed).
- The clues are generated by OpenAI and help to find the word.
- Accents are filled in automatically and are not considered in the hints.
